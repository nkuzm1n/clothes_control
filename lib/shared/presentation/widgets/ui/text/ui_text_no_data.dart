import 'package:flutter/material.dart';

class UiTextNoData extends StatelessWidget {
  const UiTextNoData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Нет данных'));
  }
}
